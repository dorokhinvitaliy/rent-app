{
  "name": "rent-app",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "npm run build -w @rent/shared && npm run build -w @rent/api && concurrently -k -n api,web \"npm run dev -w @rent/api\" \"npm run dev -w @rent/web\"",
    "build": "npm run build -w @rent/shared && npm run build -w @rent/api && npm run build -w @rent/web",
    "start": "npm run start -w @rent/api",
    "test": "npm run build -w @rent/shared && npm run build -w @rent/api && tsx --test tests/*.test.ts"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3",
    "tsx": "^4.20.6",
    "@types/node": "^24.0.0",
    "@playwright/test": "^1.55.0"
  },
  "overrides": {
    "multer": "^2.3.0",
    "exceljs": {
      "uuid": "^11.1.1"
    }
  }
}
